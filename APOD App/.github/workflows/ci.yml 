name: iOS CI

# Trigger workflow on push or pull request to main branch
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      # 1️⃣ Checkout the repo
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Select Xcode
      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      # 3️⃣ Install CocoaPods (if using CocoaPods)
      - name: Install CocoaPods dependencies
        run: |
          sudo gem install cocoapods
          pod install --project-directory="./APOD App"

      # 4️⃣ Build the app
      - name: Build App
        run: |
          xcodebuild \
            -workspace "./APOD App/APOD App.xcworkspace" \
            -scheme "APOD App" \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 14' \
            build

      # 5️⃣ Run Unit Tests (optional)
      - name: Run Tests
        run: |
          xcodebuild \
            -workspace "./APOD App/APOD App.xcworkspace" \
            -scheme "APOD App" \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 14' \
            test
